<Window x:Class="UTF.UI.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="用户权限管理 - Universal Test Framework" 
        Height="600" Width="900" 
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题和工具栏 -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="2" Opacity="0.1" Color="Gray"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="👥" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="用户权限管理" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                    <Border Background="#E74C3C" CornerRadius="10" Padding="8,2" Margin="15,0,0,0">
                        <TextBlock x:Name="UserCountText" Text="用户总数: 0" Foreground="White" FontSize="12" FontWeight="Bold"/>
                    </Border>
                </StackPanel>
                
                <!-- 工具按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="RefreshBtn" Content="🔄 刷新" Style="{StaticResource ActionButton}" Background="#3498DB" Click="RefreshBtn_Click"/>
                    <Button x:Name="AddUserBtn" Content="➕ 添加用户" Style="{StaticResource ActionButton}" Background="#27AE60" Click="AddUserBtn_Click"/>
                    <Button x:Name="ExportUsersBtn" Content="📤 导出用户" Style="{StaticResource ActionButton}" Background="#9B59B6" Click="ExportUsersBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 主内容区域 -->
        <Border Grid.Row="1" Background="White" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="2" Opacity="0.1" Color="Gray"/>
            </Border.Effect>
            
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- 筛选区域 -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="筛选:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox x:Name="RoleFilterCombo" Width="120" Margin="0,0,10,0">
                        <ComboBoxItem Content="全部角色" IsSelected="True"/>
                        <ComboBoxItem Content="超级管理员"/>
                        <ComboBoxItem Content="管理员"/>
                        <ComboBoxItem Content="工程师"/>
                        <ComboBoxItem Content="技术员"/>
                        <ComboBoxItem Content="操作员"/>
                        <ComboBoxItem Content="观察者"/>
                    </ComboBox>
                    
                    <ComboBox x:Name="StatusFilterCombo" Width="100" Margin="0,0,10,0">
                        <ComboBoxItem Content="全部状态" IsSelected="True"/>
                        <ComboBoxItem Content="活跃"/>
                        <ComboBoxItem Content="禁用"/>
                    </ComboBox>
                    
                    <Button Content="应用筛选" Style="{StaticResource ActionButton}" Background="#3498DB" Click="ApplyFilter_Click"/>
                </StackPanel>
                
                <!-- 用户列表 -->
                <DataGrid Grid.Row="1" x:Name="UserDataGrid" 
                        AutoGenerateColumns="False" 
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        GridLinesVisibility="Horizontal"
                        HeadersVisibility="Column"
                        AlternatingRowBackground="#F8F9FA">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="100"/>
                        <DataGridTextColumn Header="显示名称" Binding="{Binding DisplayName}" Width="120"/>
                        <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" Width="180"/>
                        <DataGridTemplateColumn Header="角色" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="3" Padding="6,2">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RoleDisplayName}" Value="超级管理员">
                                                        <Setter Property="Background" Value="#E74C3C"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RoleDisplayName}" Value="管理员">
                                                        <Setter Property="Background" Value="#E67E22"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RoleDisplayName}" Value="工程师">
                                                        <Setter Property="Background" Value="#3498DB"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RoleDisplayName}" Value="技术员">
                                                        <Setter Property="Background" Value="#27AE60"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RoleDisplayName}" Value="操作员">
                                                        <Setter Property="Background" Value="#F39C12"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RoleDisplayName}" Value="观察者">
                                                        <Setter Property="Background" Value="#95A5A6"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding RoleDisplayName}" Foreground="White" FontWeight="Bold" TextAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="状态" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="3" Padding="5,2">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding StatusDisplayName}" Value="活跃">
                                                        <Setter Property="Background" Value="#27AE60"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusDisplayName}" Value="禁用">
                                                        <Setter Property="Background" Value="#E74C3C"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding StatusDisplayName}" Foreground="White" FontWeight="Bold" TextAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAtDisplayName}" Width="120"/>
                        <DataGridTextColumn Header="最后登录" Binding="{Binding LastLoginDisplayName}" Width="120"/>
                        <DataGridTemplateColumn Header="操作" Width="250">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="编辑" Style="{StaticResource ActionButton}" Background="#3498DB" Width="50" Click="EditUser_Click"/>
                                        <Button Content="权限" Style="{StaticResource ActionButton}" Background="#9B59B6" Width="50" Click="ManagePermissions_Click"/>
                                        <Button Content="重置密码" Style="{StaticResource ActionButton}" Background="#F39C12" Width="70" Click="ResetPassword_Click"/>
                                        <Button Content="禁用" Style="{StaticResource ActionButton}" Background="#E74C3C" Width="50" Click="DisableUser_Click"/>
                                        <Button Content="删除" Style="{StaticResource ActionButton}" Background="#C0392B" Width="50" Click="DeleteUser_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
        
        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15,10" Margin="0,10,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="2" Opacity="0.1" Color="Gray"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusTextBlock" Grid.Column="0" Text="就绪" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="当前用户:" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="CurrentUserText" Text="admin" FontWeight="Bold" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <Button Content="关闭" Style="{StaticResource ActionButton}" Background="#95A5A6" Click="CloseBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
